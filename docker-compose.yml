version: "3.9"
services:
  gmail-cleaner-web:
    build: .
    container_name: gmail-cleaner-web
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
